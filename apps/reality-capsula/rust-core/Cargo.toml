[package]
name = "capsula-rust-core"
version = "1.0.0"
edition = "2021"
description = "MESIE Reality Capsula — φ design score + spectral validate (Rust)"

[[bin]]
name = "capsula-rust-core"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
